<%- include('partials/header', { title }) %>

<section class="page-title">
    <p class="eyebrow">Booking confirmed</p>
    <h1>Your booking request was saved.</h1>
    <p>
        <strong><%= service.name %></strong> at <strong><%= merchant.name %></strong>
        on <strong><%= bookingDate %></strong> at <strong><%= bookingTime %></strong>.
    </p>
    <a class="button primary" href="/booking/<%= merchant.id %>">Create another booking</a>
</section>

<%- include('partials/footer') %>
